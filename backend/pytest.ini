[pytest]
# --ds takes precedence over the container-level DJANGO_SETTINGS_MODULE env var
# (api.settings.dev), ensuring tests always run against the test settings module.
addopts = --ds=api.settings.test
DJANGO_SETTINGS_MODULE = api.settings.test
python_files = tests.py test_*.py *_tests.py
testpaths = ./tests
