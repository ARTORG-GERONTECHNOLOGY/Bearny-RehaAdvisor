# Debugging & Django settings
DEBUG=True
SECRET_KEY=biglongdevsecretstuffhere
DJANGO_ALLOWED_HOSTS=dev.reha-advisor.ch localhost 127.0.0.1 0.0.0.0 [::1] django nginx

# Used in templates or media URLs
MEDIA_HOST=https://dev.reha-advisor.ch
EMAIL_HOST_USER=info@reha-advisor.com
# Frontend-to-backend API URL (e.g. VITE)
VITE_API_URL=https://dev.reha-advisor.ch/api

# MongoDB configuration
DB_HOST=db
DB_PORT=27017
DB_NAME=reha_advisor_dev
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=example

# Optional: For local dev behavior
DISABLE_ESLINT_PLUGIN=true

# Point to dev Django settings
DJANGO_SETTINGS_MODULE=api.settings.dev

# HTTPS redirection (off for local dev)
SECURE_SSL_REDIRECT=false
EMAIL_HOST=smtp.office365.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False

EMAIL_HOST_USER=info@reha-advisor.ch
EMAIL_HOST_PASSWORD=ntdtfnyglrfmbdxx
#nrk37CAYsTW&3sJ outlook